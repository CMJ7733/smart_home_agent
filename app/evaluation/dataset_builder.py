# TODO (Phase 4, placeholder): build a fine-tune dataset from bad cases.
# Runtime inference now uses local Ollama; any future fine-tune base model is intentionally left open.


def build_finetune_dataset(bad_case_dir: str, output_path: str) -> None:
    raise NotImplementedError("Phase 4 (placeholder): fine-tune dataset builder is not implemented yet.")
